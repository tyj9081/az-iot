<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aziot.dao.mapper.device.DevDeviceAlarmConfigMapper">

    <select id="selectByDeviceId" resultType="com.aziot.dao.entity.device.DevDeviceAlarmConfig">
        SELECT * FROM dev_device_alarm_config
        WHERE device_id = #{deviceId}
    </select>

    <select id="selectByDeviceSensorType" resultType="com.aziot.dao.entity.device.DevDeviceAlarmConfig">
        SELECT * FROM dev_device_alarm_config
        WHERE device_id = #{deviceId}
          AND sensor_code = #{sensorCode}
          AND alarm_type = #{alarmType}
    </select>

    <delete id="deleteByDeviceAndSensor">
        DELETE FROM dev_device_alarm_config
        WHERE device_id = #{deviceId} AND sensor_code = #{sensorCode}
    </delete>

    <select id="countEnabled" resultType="long">
        SELECT COUNT(*) FROM dev_device_alarm_config WHERE alarm_enabled = 1
    </select>

</mapper>
