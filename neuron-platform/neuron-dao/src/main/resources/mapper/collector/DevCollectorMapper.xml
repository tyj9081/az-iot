<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aziot.dao.mapper.collector.DevCollectorMapper">

    <select id="selectPageByCondition" resultType="com.aziot.dao.entity.collector.DevCollector">
        SELECT * FROM dev_collector
        <where>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (name LIKE CONCAT('%', #{keyword}, '%')
                 OR code LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY id ASC
    </select>

    <select id="selectByCodeExcludeId" resultType="com.aziot.dao.entity.collector.DevCollector">
        SELECT * FROM dev_collector
        WHERE code = #{code} AND id != #{excludeId}
    </select>

    <select id="selectByCode" resultType="com.aziot.dao.entity.collector.DevCollector">
        SELECT * FROM dev_collector WHERE code = #{code}
    </select>

    <select id="selectByMqttUsername" resultType="com.aziot.dao.entity.collector.DevCollector">
        SELECT * FROM dev_collector WHERE mqtt_username = #{mqttUsername}
    </select>

</mapper>
