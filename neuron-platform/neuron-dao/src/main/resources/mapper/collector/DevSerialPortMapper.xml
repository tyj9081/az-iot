<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aziot.dao.mapper.collector.DevSerialPortMapper">

    <select id="selectByCollectorId" resultType="com.aziot.dao.entity.collector.DevSerialPort">
        SELECT * FROM dev_serial_port
        WHERE collector_id = #{collectorId}
        ORDER BY id ASC
    </select>

    <select id="countByCollectorId" resultType="long">
        SELECT COUNT(*) FROM dev_serial_port WHERE collector_id = #{collectorId}
    </select>

</mapper>
